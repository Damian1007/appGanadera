import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.appGanadera',
  appName: 'appGanadera',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
